class GameEvent:
    def __init__(self, name, effect):
        self.name = name
        self.effect = effect
    
    def apply(self, game_state):
        self.effect(game_state)
    
    def to_dict(self):
        return {
            "name": self.name
        }